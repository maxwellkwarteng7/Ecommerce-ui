export interface registerTemplate {
    username: string, 
    password: string, 
    email: string, 
    role : string , 
}

export interface loginTemplate {
    email: string, 
    password : string   
}